/*
 * v60 external init.elf source note
 *
 * user/init.elf is generated by user/build_init_elf.py.
 * It exercises:
 *   write(stdout)
 *   fstat(stdout)
 *   lseek(stdout)
 *   openat("/dev/null") / write / close
 *   openat("/dev/zero") / read / close
 *   openat("/dev") / getdents64 / close
 *   brk(0) / brk(0x40031000)
 *   getpid/getppid/unsupported/exit
 */
