# Iteration 01 Third-Party Delta

No new third-party source code, libraries, binaries, or documentation were added.

Local tooling used for inspection:

- `debugfs` to extract real files from the official LoongArch sdcard image copy.
- `rust-objdump` and `strings` to inspect syscall behavior in real `/musl/basic/*` ELFs.
- QEMU via the official LoongArch command shape.

No kernel binaries, sdcard images, target directories, or large raw logs were stored in this iteration directory.

