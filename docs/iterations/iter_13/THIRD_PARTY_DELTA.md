# Iteration 13 Third-Party Delta

No third-party code, dependencies, binaries, disk images, target directories, or large raw logs were added.

The iteration reused:

- Existing project source.
- Existing official sdcard content from the local evaluation environment.
- Existing local official BusyBox judge script for command-key inspection.
- Existing local tools for validation: `cargo`, `make`, `file`, `qemu-system-loongarch64`, `docker`, and `grep`.

The temporary local LoongArch sdcard image used for smoke testing was created under `/tmp` and removed after validation. No `.docx` files were generated.

