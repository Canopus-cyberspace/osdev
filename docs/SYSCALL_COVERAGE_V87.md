# Syscall coverage guard v87

This file is generated by `v87_generic_syscall_coverage_guard_fix`.

Scope:

- Preserve the v86 passing baseline.
- Verify modern Linux generic syscall scaffold constants and dispatcher arms through `rseq_slice_yield(471)`.
- Reject duplicate dispatcher arms before Rust emits `unreachable pattern`.
- Keep the external init smoke marker synchronized as:

```text
hello from external init.elf v87 syscall write
```

Run manually from the project root:

```bash
python3 tools/check_syscall_scaffold_v87.py
```
